<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Launch the rule wizard against a stored incident.  The most recent triage is preselected; pick a different one below to author a rule from an older session."
                   FontSize="12"
                   Foreground="{DynamicResource MahApps.Brushes.Gray1}"
                   TextWrapping="Wrap"
                   Margin="4,4,4,12"/>

        <!-- Incident picker row -->
        <Grid Grid.Row="1" Margin="4,0,4,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Source incident"
                       VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold"
                       Foreground="{DynamicResource MahApps.Brushes.Gray3}"/>
            <ComboBox  Grid.Column="1" x:Name="cmbWizIncident"
                       FontSize="12" Margin="0,0,8,0"
                       Style="{DynamicResource MahApps.Styles.ComboBox}"/>
            <Button    Grid.Column="2" x:Name="btnWizRefresh" Content="Refresh"
                       MinWidth="90" Height="30"
                       Style="{DynamicResource MahApps.Styles.Button.Square}"
                       Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
        </Grid>

        <!-- Launch button row -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="4,0,4,4">
            <Button x:Name="btnWizLaunch" Content="Launch wizard..."
                    MinWidth="160" Height="32" Margin="0,0,8,0" IsEnabled="False"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    Controls:ControlsHelper.ContentCharacterCasing="Normal"/>
            <TextBlock x:Name="txtWizHint"
                       VerticalAlignment="Center" FontSize="11"
                       Foreground="{DynamicResource MahApps.Brushes.Gray1}"
                       Text="Run a triage first to capture an incident the wizard can seed from."/>
        </StackPanel>
    </Grid>
</UserControl>
